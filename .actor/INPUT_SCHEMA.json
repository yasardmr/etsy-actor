{
  "title": "Etsy Search Results Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "query": {
      "title": "Search Query",
      "type": "string",
      "description": "What to search for on Etsy (e.g., 'handmade jewelry', 'vintage watch', 'ceramic mug')",
      "prefill": "handmade jewelry",
      "editor": "textfield"
    },
    "searchUrl": {
      "title": "Search URL (with filters)",
      "type": "string",
      "description": "Full Etsy search URL including filters and sort (e.g. is_best_seller, order=date_desc). When set, the actor opens this exact URL instead of typing only the q= keyword.",
      "editor": "textfield",
      "prefill": "https://www.etsy.com/search?q=lamp&instant_download=false&explicit=1&is_best_seller=true&page=1&order=date_desc&ref=pagination"
    },
    "searchUrls": {
      "title": "Multiple search URLs",
      "type": "array",
      "description": "Optional list of Etsy search URLs to scrape one after another (each can use maxPages for pagination).",
      "editor": "stringList",
      "items": {
        "type": "string"
      }
    },
    "maxPages": {
      "title": "Maximum search pages",
      "type": "integer",
      "description": "How many result pages to open per search (page=1, page=2, ...). Stops early when maxItems is reached or a page has no listings.",
      "default": 1,
      "minimum": 1,
      "maximum": 50,
      "editor": "number"
    },
    "maxItems": {
      "title": "Maximum Results",
      "type": "integer",
      "description": "Maximum number of products to extract from search results (typical: 60-100 per search page)",
      "default": 100,
      "minimum": 1,
      "maximum": 500,
      "editor": "number",
      "prefill": 100
    },
    "minRating": {
      "title": "Minimum Rating",
      "type": "integer",
      "description": "Filter products by minimum rating (1-5 stars)",
      "minimum": 0,
      "maximum": 5,
      "editor": "number"
    },
    "minReviews": {
      "title": "Minimum Reviews",
      "type": "integer",
      "description": "Filter products by minimum number of reviews",
      "minimum": 0,
      "editor": "number"
    },
    "priceMin": {
      "title": "Minimum Price",
      "type": "integer",
      "description": "Filter products by minimum price (USD)",
      "minimum": 0,
      "editor": "number"
    },
    "priceMax": {
      "title": "Maximum Price",
      "type": "integer",
      "description": "Filter products by maximum price (USD)",
      "minimum": 0,
      "editor": "number"
    }
  },
  "required": [],
  "additionalProperties": false
}
